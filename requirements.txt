# Core dependencies
websockets>=12.0
pyyaml>=6.0
numpy>=1.24.0

# PyTorch (install with CUDA support for GPU acceleration)
# Install manually with: pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu118
torch>=2.0.0
torchaudio>=2.0.0

# Transcription
faster-whisper>=1.0.0

# Speaker diarization
pyannote.audio>=3.1.0

# Development
pytest>=7.0.0
pytest-asyncio>=0.21.0
